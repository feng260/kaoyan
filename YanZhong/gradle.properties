org.gradle.jvmargs=-Xmx1536m -Dfile.encoding=UTF-8
# 系统页面文件被禁用时,限制 Kotlin 编译守护进程堆,避免预留内存失败(errno 1455)
kotlin.daemon.jvmargs=-Xmx1024m
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# ---- 发布签名 ----
# 口令不写在本文件里(本文件会提交到仓库,口令一旦泄露,任何人都能签出可覆盖安装的伪造升级包)。
# 实际口令放在同目录的 keystore.properties,该文件已被 .gitignore 忽略,换机器时手工同步。
# 也支持用环境变量 YANZHONG_STORE_PASS / YANZHONG_KEY_PASS 注入,方便 CI。
